require 'spec_helper'

describe ComposableDecorator do
  it 'has a version number' do
    expect(ComposableDecorator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
